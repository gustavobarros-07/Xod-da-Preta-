# ==========================================
# VARIAVEIS DE AMBIENTE - Xodo da Preta
# ==========================================
#
# INSTRUCOES:
# 1. Copie este arquivo para .env
# 2. Preencha com valores reais (nunca commitar valores sensiveis)
# 3. Configure ADMIN_PASSWORD via variavel de ambiente no deploy
#
# ==========================================

# SEGURANCA
# Chave secreta para sessoes Flask (OBRIGATORIO)
# Gere uma chave forte com: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=sua-chave-secreta-de-64-caracteres-aqui

# ADMIN - Credenciais do primeiro usuario administrador (OBRIGATORIO)
ADMIN_USERNAME=admin
# Defina a senha em tempo de deploy/export e nao deixe neste arquivo versionado
ADMIN_PASSWORD=defina_no_deploy

# AMBIENTE
# Opcoes: development, production
FLASK_ENV=development

# DEBUG
# Ativar apenas em desenvolvimento (True/False)
FLASK_DEBUG=True

# BANCO DE DADOS
# SQLite eh usado por padrao, o arquivo sera criado em instance/loja.db
# Para producao, considere PostgreSQL ou MySQL

